<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.taozi.mapper.ProductMapper">

    <select id="findAll" resultType="com.taozi.entity.Product">
        SELECT * FROM product ORDER BY id DESC
    </select>

    <select id="findByCategoryId" resultType="com.taozi.entity.Product">
        SELECT * FROM product WHERE category_id = #{categoryId} ORDER BY id DESC
    </select>

    <select id="findById" resultType="com.taozi.entity.Product">
        SELECT * FROM product WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.taozi.entity.Product" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO product (name, description, price, stock, image, category_id)
        VALUES (#{name}, #{description}, #{price}, #{stock}, #{image}, #{categoryId})
    </insert>

    <update id="update" parameterType="com.taozi.entity.Product">
        UPDATE product
        SET name=#{name}, description=#{description}, price=#{price},
            stock=#{stock}, image=#{image}, category_id=#{categoryId}
        WHERE id=#{id}
    </update>

    <delete id="delete">
        DELETE FROM product WHERE id=#{id}
    </delete>

</mapper>
